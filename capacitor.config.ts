import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'PrimeraAplicacion',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
